# 1.4.1 跳跃辅助精简计划

用户明确要求仅保留自动跳跃和 BHopJump，透视不变。沿用已有 F3 / 空格触发、自动配置和退出恢复流程，直接按已授权范围执行。目录无 Git，不创建提交。

设计：保留原配置 / 协议编号 AutoJump=0、BhopJump=4，界面显示两个 radio，通过映射避免以协议编号作为界面下标。其他编号拒绝执行，旧 INI 选择回退自动跳跃并提示。移除自动摆角、长跳、MCJ、双蹲及专用 PlayerPostThink 视角同步；保留手动转向配合侧移和落地续跳，原 ESP 文件及 F8/F9/F11 行为不改。

- [x] 更新失败测试：协议拒绝 1/2/3/5，旧 BHopJump 编号 4 保留；INI 回退；真实界面恰好两个模式；SDK 无被移除动作或强制视角。
- [x] 精简 shared/jump_modes.h、plugin/kz_movement.* 和 plugin.cpp；main.cpp 使用两个模式映射并收紧面板；保留中断清理与模式代次。
- [x] 更新版本 1.4.1、README 和发布说明；独立复核；Debug/Release 全量回归，核对 ESP 源码与 1.4.0 一致。
- [x] 打包 1.4.1，检验文件哈希及包内 EXE 的两个 radio、偏好记忆、自动配置和退出恢复；交付 ZIP。
